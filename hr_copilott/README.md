# HR Copilot (Final Debug Version)

✅ Inclusief:
- `/ping` healthcheck
- `/favicon.ico` dummy route
- app.py entry point voor Railway

## Start command (Railway)
```
uvicorn app:app --host=0.0.0.0 --port=8000
```